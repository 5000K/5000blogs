services:
  blog:
    build: .
    ports:
      - "8080:8080"
    environment:
      CONFIG_PATH: /config.yml
    volumes:
      - ./config.docker-comp.yml:/config.yml:ro
      - ./docs:/docs:ro
      - ./template:/static:ro
